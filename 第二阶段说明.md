# 第二阶段：训练 RepViT 氛围评价模型

## 📁 文件清单

已创建以下文件：

| 文件 | 说明 |
|------|------|
| `dataset.py` | 数据集加载类 |
| `model.py` | RepViT 多任务模型 |
| `train.py` | 训练脚本 |
| `test.py` | 测试脚本 |
| `install_pytorch.bat` | PyTorch 安装脚本 |

## 🚀 使用步骤

### 步骤1：安装 PyTorch

双击运行 `install_pytorch.bat`，根据提示选择版本：
- 有 NVIDIA GPU → 选择 CUDA 版本（推荐）
- 无 GPU → 选择 CPU 版本

或手动安装：
```bash
# GPU版本 (CUDA 11.8)
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# CPU版本
pip install torch torchvision torchaudio
```

### 步骤2：安装其他依赖

```bash
pip install timm pandas pillow tqdm
```

### 步骤3：训练模型

```bash
cd C:\Users\admin\Desktop\RepViT
python train.py
```

训练参数：
- Batch Size: 8（根据GPU显存调整）
- Learning Rate: 3e-4
- Epochs: 50
- 优化器: AdamW
- 学习率调度: CosineAnnealing

### 步骤4：测试模型

```bash
python test.py
```

## 📊 数据集说明

| 数据集 | 数量 | 用途 |
|--------|------|------|
| train.csv | 2976 | 训练 |
| val.csv | 851 | 验证 |
| test.csv | 430 | 测试 |

## 🎯 模型输出

- **分类输出**: 7种氛围类别（科技感、时尚感、喜庆感、简约商务、复古国风、卡通童趣、高端轻奢）
- **回归输出**: 5个评分（颜色、构图、风格、情感、商业属性）

## 💾 模型保存

- `outputs/best_model.pth` - 最佳模型
- `outputs/model_epoch_X.pth` - 每10个epoch保存

## ⚠️ 注意事项

1. 如果 GPU 显存不足，减小 `BATCH_SIZE`
2. Windows 下 `NUM_WORKERS` 建议设为 0
3. 预训练权重需要下载：`weights/repvit_m_1.0.pth`
